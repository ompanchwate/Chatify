/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {hostname: "majestic-vole-679.convex.cloud"}
        ]
    }
};

export default nextConfig;
